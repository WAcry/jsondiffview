[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "json-diff-cli"
version = "0.1.0"
dependencies = ["PyYAML>=6.0"]

[project.scripts]
json-diff = "json_diff_cli.cli:main"
